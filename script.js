/**
 *Fonction pour calculer cube d'un nombre
 * @param {number} number
 * @returns
 */

function cube(number) {
  return number * number * number;
}

console.log(cube(3));
console.log(cube(5));
console.log(cube(10));
